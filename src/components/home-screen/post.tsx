import React from 'react'
import { Post } from '@/types/post'

const post = ({post,isSubscribed,admin}:{post:Post,isSubscribed:boolean,admin:any}) => {
  return (
    <div className="flex flex-col gap-3 p-3 border-t">post</div>
  )
}

export default post